#include <stdio.h>

int main()
{
    ClassNode* root = NULL;

    insertClass(&root, "1-A");

    Student* s1 =
        createStudent(
            2025001,
            "Kim",
            90,
            80,
            95);

    addStudent(root, s1);

    printf("반 평균 : %.2f\n",
        calculateClassAverage(root));

    printExcellentStudents(root);

    printRiskStudents(root);

    return 0;
}
